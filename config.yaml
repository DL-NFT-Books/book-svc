log:
  disable_sentry: true

db:
  url: "postgres://book:book@localhost:25432/book?sslmode=disable"

listener:
  addr: :8000

cop:
  disabled: true
  endpoint: "http://..."
  upstream: "http://..."
  service_name: book-svc
  service_port: 80
